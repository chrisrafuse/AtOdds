[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m apps.web.run_api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "atodds-api"
