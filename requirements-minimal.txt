# Minimal requirements for Python 3.14 compatibility (skip pydantic-core)
# Use with LLM_PROVIDER=mock for testing without API keys

# Core web framework
uvicorn[standard]==0.27.0
starlette==0.35.1

# Data validation (use older version compatible with Python 3.14)
# Note: pydantic-core fails to build on Python 3.14; using pydantic v2 without core
pydantic==2.7.4

# File uploads
python-multipart==0.0.6

# Environment variables
python-dotenv==1.0.0

# LLM providers (optional)
openai>=1.0.0
anthropic>=0.8.0
google-generativeai>=0.7.0
